@model IEnumerable<Repartidor2.Models.TblTipoProducto>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.NombreTipoProducto)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.DescripcionTipoProducto)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.ActivoTipoProducto)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.NombreTipoProducto)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.DescripcionTipoProducto)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.ActivoTipoProducto)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.IdTipoProducto">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.IdTipoProducto">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.IdTipoProducto">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
